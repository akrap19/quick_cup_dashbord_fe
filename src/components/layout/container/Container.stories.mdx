import { Container } from './Container'
import { Canvas, Meta } from '@storybook/addon-docs'

<Meta title="UI/Layout/Container/Docs" />

# Container

Provides a container that both centers and constrains the maximum width of the content it wraps.

# Max width

Use the `width` prop to adjust the maximum width of the container.

```jsx
<Container width="large">...</Container>
```
