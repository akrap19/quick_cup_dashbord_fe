// import classnames from 'classnames'
// import { Children, PropsWithChildren, ReactNode } from 'react'

// import type { Atoms } from 'style/atoms.css'

// import { type TilesAtoms, tilesAtoms } from './Tiles.css'
// import { Box } from '../box/Box'

// type Props = Pick<TilesAtoms, 'columns'> & Pick<Atoms, 'gap'> & { children: Array<JSX.Element> }

// export const Tiles = ({ children, gap = 'medium', columns = 1 }: Props) => {
// 	return (
// 		<Box display="flex" flexWrap="wrap" gap={gap}>
// 			{children.map((child, index) => (
// 				// eslint-disable-next-line react/no-array-index-key
// 				<div key={index} className={classnames(tilesAtoms({ columns }))}>
// 					{child}
// 				</div>
// 			))}
// 		</Box>
// 	)
// }

export {}
